import { CAPITALS_PACK } from './capitals';
import { FLAGS_PACK } from './flags';
import type { ContentPack } from './types';

/** Subjects selectable in the menu. Adding one = adding a pack here. */
export const PACKS: ContentPack[] = [FLAGS_PACK, CAPITALS_PACK];
